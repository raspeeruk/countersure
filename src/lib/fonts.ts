/**
 * Font loaders.
 *
 * Geist Sans + Geist Mono come from the `geist` package and are imported
 * directly in `app/layout.tsx`.
 *
 * Cabinet Grotesk is the body font per DESIGN.md but ships from Fontshare —
 * download the WOFF2s and place them in `/public/fonts/`, then enable the
 * loader below.
 */
export {};
